import { Skeleton } from "@/frontend/components/ui/Skeleton";
import { PackageOpen } from "lucide-react";

export default function DashboardLoading() {
    return (
        <div className="max-w-[1600px] mx-auto p-6 space-y-6 pb-20 w-full animate-in fade-in duration-500">
            {/* Header Skeleton */}
            <div className="mb-8">
                <div className="flex items-center justify-between mb-2">
                    <div className="flex items-center gap-3">
                        <div className="p-3 bg-slate-100 rounded-xl border border-slate-200 animate-pulse">
                            <PackageOpen className="w-8 h-8 text-slate-300" />
                        </div>
                        <div className="space-y-2">
                            <Skeleton className="h-8 w-64 bg-slate-300" />
                            <Skeleton className="h-4 w-96 bg-slate-200" />
                        </div>
                    </div>
                    <div className="flex gap-2">
                        <Skeleton className="h-10 w-24 rounded-lg bg-slate-200" />
                        <Skeleton className="h-10 w-32 rounded-lg bg-slate-200" />
                    </div>
                </div>
            </div>

            {/* Filters Skeleton */}
            <div className="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
                <Skeleton className="h-12 w-full rounded-xl bg-slate-200" />
                <Skeleton className="h-12 w-full rounded-xl bg-slate-200" />
                <Skeleton className="h-12 w-full rounded-xl bg-slate-200" />
                <Skeleton className="h-12 w-full rounded-xl bg-slate-200 bg-blue-100/50" />
            </div>

            {/* Summary Stats Skeleton */}
            <div className="grid grid-cols-2 lg:grid-cols-6 gap-4 mb-8">
                {Array.from({ length: 6 }).map((_, i) => (
                    <div key={i} className="bg-white p-4 rounded-xl border border-slate-200 shadow-sm space-y-3">
                        <Skeleton className="h-3 w-16 bg-slate-300" />
                        <Skeleton className="h-8 w-full bg-slate-200" />
                    </div>
                ))}
            </div>

            {/* Table Header/Body Skeleton */}
            <div className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden flex flex-col">
                <div className="divide-y divide-slate-100">
                    <table className="w-full text-left border-collapse table-fixed min-w-[1400px]">
                        <thead>
                            <tr className="border-b border-slate-200 bg-slate-50">
                                {Array.from({ length: 12 }).map((_, i) => (
                                    <th key={i} className="px-6 py-4">
                                        <Skeleton className="h-3 w-3/4 mx-auto" />
                                    </th>
                                ))}
                            </tr>
                        </thead>
                        <tbody>
                            {Array.from({ length: 7 }).map((_, idx) => (
                                <tr key={idx} className="animate-pulse">
                                    {Array.from({ length: 12 }).map((_, i) => (
                                        <td key={i} className="px-6 py-4">
                                            <Skeleton className="h-4 w-full bg-slate-100" />
                                        </td>
                                    ))}
                                </tr>
                            ))}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    );
}
